{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "//": "Metamask locks chainIDs on port 8545 https://hardhat.org/hardhat-network/docs/metamask-issue",
    "chain": "hardhat node --port 8546",
    "test:single-test": "npm run build && playwright test -c tests/single-test/playwright.config.ts tests/single-test",
    "test:multiple-files": "npm run build && playwright test -c tests/multiple-files/playwright.config.ts tests/multiple-files",
    "test:multiple-wallets": "npm run build && playwright test -c tests/multiple-wallets/playwright.config.ts tests/multiple-wallets"
  },
  "dependencies": {
    "dotenv": "^16.4.4",
    "ethers": "^6.0.8",
    "@faker-js/faker": "^8.3.1",
    "playwright-core": "^1.41.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0",
    "@tenkeylabs/dappwright": "^2.8.0",
    "hardhat": "^2.13.0",
    "typescript": "^4.9.3",
    "vite": "^4.1.5"
  }
}
